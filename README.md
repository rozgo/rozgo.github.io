# rozgo.github.io

Portfolio site of Alex Rozgo, simulation and AI engineer: https://rozgo.github.io/

Static HTML, CSS and JavaScript with no build step. Preview with `python3 -m http.server 8000` and check links with `python3 scripts/check_links.py --youtube`. See `CLAUDE.md` for the layout of the repository.
